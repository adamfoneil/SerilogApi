# Copilot Instructions

## Project Guidelines
- User prefers a simpler approach for log override table creation: keep SQL in LogOverrideConfiguration.CreateIfNotExistsSql and call it directly rather than dynamic migration-SQL generation.